'use client'
import { useEffect } from 'react'
export default function Scanner({onDetected}:{onDetected:(code:string)=>void}){useEffect(()=>{let html5QrCode:any;let stopped=false;async function start(){const {Html5Qrcode,Html5QrcodeSupportedFormats}=await import('html5-qrcode');html5QrCode=new Html5Qrcode('reader');await html5QrCode.start({facingMode:'environment'},{fps:10,qrbox:{width:260,height:150},formatsToSupport:[Html5QrcodeSupportedFormats.EAN_13,Html5QrcodeSupportedFormats.EAN_8,Html5QrcodeSupportedFormats.UPC_A,Html5QrcodeSupportedFormats.CODE_128]},(txt:string)=>{if(!stopped){stopped=true;onDetected(txt);html5QrCode.stop().catch(()=>{})}},()=>{})}start().catch(()=>alert('Não foi possível abrir a câmera. Use HTTPS e permita a câmera.'));return()=>{stopped=true;if(html5QrCode)html5QrCode.stop().catch(()=>{})}},[onDetected]);return <div id="reader" className="mt-3 overflow-hidden rounded-xl"/>}
